@model ChemisTrackCrud.Models.ClaimsModel

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

    <div class="form-group clearfix">
        <div class="col-sm-4 text-right">@Html.DisplayNameFor(model => model.StudentID) :</div>
        <div class="col-sm-6">@Html.DisplayFor(model => model.StudentID)</div>
    </div>

    <div class="form-group clearfix">
        <div class="col-sm-4 text-right">@Html.DisplayNameFor(model => model.EquipmentID) :</div>
        <div class="col-sm-6">@Html.DisplayFor(model => model.EquipmentID)</div>
    </div>

    <div class="form-group clearfix">
        <div class="col-sm-4 text-right">@Html.DisplayNameFor(model => model.Quantity) :</div>
        <div class="col-sm-6">@Html.DisplayFor(model => model.Quantity)</div>
    </div>

<p class="col-sm-8 text-center">
    @Html.ActionLink("Edit", "Edit", new { id=Model.ClaimID }) |
    @Html.ActionLink("Back to List", "Index", null, new { @class="btn btn-default"})
</p>
